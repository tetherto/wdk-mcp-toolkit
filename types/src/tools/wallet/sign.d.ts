/**
 * Registers the 'sign' tool for signing messages.
 *
 * @param {import('../../server.js').WdkMcpServer} server - The MCP server instance.
 * @returns {void}
 */
export function sign(server: import("../../server.js").WdkMcpServer): void;
