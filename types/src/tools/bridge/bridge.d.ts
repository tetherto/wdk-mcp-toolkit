/**
 * Registers the 'bridge' tool for executing cross-chain bridge operations.
 *
 * @param {import('../../server.js').WdkMcpServer} server - The MCP server instance.
 * @returns {void}
 */
export function bridge(server: import("../../server.js").WdkMcpServer): void;
