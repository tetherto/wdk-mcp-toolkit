/**
 * Registers the 'sell' tool for selling crypto for fiat currency.
 *
 * @param {import('../../server.js').WdkMcpServer} server - The MCP server instance.
 * @returns {void}
 */
export function sell(server: import("../../server.js").WdkMcpServer): void;
