/**
 * Registers the 'buy' tool for purchasing crypto with fiat currency.
 *
 * @param {import('../../server.js').WdkMcpServer} server - The MCP server instance.
 * @returns {void}
 */
export function buy(server: import("../../server.js").WdkMcpServer): void;
